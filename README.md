# Sample Node.js application

This repository is a sample Node.js application for Docker's documentation.


# JavaScript applicarion usually have 3 steps: Install dependency, build application and run application
<!-- 
1. Application Dependency Installation 
    devoloper will give us commnad:
        - npm ci --omit=dev

2. Build the Application 
    - No application build specificly for this JavaScript, some other need to be build
    - so we are not compiling anything, we can skip this step.

3. Run the application 
    we ask devolopers, how do we run the application:
        - node src/index.js -->
